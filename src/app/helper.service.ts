import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class HelperService {

  constructor() { }

  // searchQueryInUsers(queryToSearch: string):
  //  Customer[]{
  //   return customerArray.filter((customer: Customer) =>{
  //     queryToSearch = queryToSearch.toLowerCase();
  //     return  customer.address.toLowerCase().includes(queryToSearch) ||
  //     customer.phone.toLowerCase().includes(queryToSearch) || 
  //     customer.email.toLowerCase().includes(queryToSearch) || 
  //     customer.notes.toLowerCase().includes(queryToSearch) ||
  //     customer.fname.toLowerCase().includes(queryToSearch) ||
  //     customer.lname.toLowerCase().includes(queryToSearch)
  //   }
  //   )}
  



}
